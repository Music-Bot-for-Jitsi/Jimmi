{
  "scripts": {
    "start": "deno run --allow-read --allow-net --config deno.json --watch src/app.ts",
    "lint": "deno lint --config deno.json",
    "fmt": "deno fmt --config deno.json"
  },
  "files": [
    "./src"
  ]
}
